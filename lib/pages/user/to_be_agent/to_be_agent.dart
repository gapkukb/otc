import 'package:flutter/material.dart';

class UserToBeAgent extends StatefulWidget {
  const UserToBeAgent({super.key});

  @override
  State<UserToBeAgent> createState() => _UserToBeAgentState();
}

class _UserToBeAgentState extends State<UserToBeAgent> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
