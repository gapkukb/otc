import 'package:flutter/material.dart';

class WalletBanks extends StatelessWidget {
  const WalletBanks({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Text("data123213"),
    );
  }
}
