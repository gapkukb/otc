import 'package:flutter/material.dart';

class Gridview2 extends GridView {
  Gridview2({super.key, required super.gridDelegate});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
