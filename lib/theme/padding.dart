import 'package:flutter/material.dart';

class Pads {
  /// all(4)
  static const mini = EdgeInsets.all(4);

  /// all(8)
  static const xs = EdgeInsets.all(8);

  /// all(16)
  static const sm = EdgeInsets.all(16);

  /// all(24)
  static const md = EdgeInsets.all(24);

  /// all(32)
  static const lg = EdgeInsets.all(32);

  /// all(40)
  static const xl = EdgeInsets.all(40);

  ///horizontal: 4
  static const xAxisMini = EdgeInsets.symmetric(horizontal: 4);

  ///horizontal: 8
  static const xAxisXs = EdgeInsets.symmetric(horizontal: 8);

  ///horizontal: 16
  static const xAxisSm = EdgeInsets.symmetric(horizontal: 16);

  ///horizontal: 24
  static const xAxisMd = EdgeInsets.symmetric(horizontal: 24);

  ///horizontal: 32
  static const xAxisLg = EdgeInsets.symmetric(horizontal: 32);

  ///horizontal: 40
  static const xAxisXl = EdgeInsets.symmetric(horizontal: 40);

  ///vertical: 4
  static const yAxisMini = EdgeInsets.symmetric(vertical: 4);

  ///vertical: 8
  static const yAxisXs = EdgeInsets.symmetric(vertical: 8);

  ///vertical: 16
  static const yAxisSm = EdgeInsets.symmetric(vertical: 16);

  ///vertical: 24
  static const yAxisMd = EdgeInsets.symmetric(vertical: 24);

  ///vertical: 32
  static const yAxisLg = EdgeInsets.symmetric(vertical: 32);

  ///vertical: 40
  static const yAxisXl = EdgeInsets.symmetric(vertical: 40);

  ///left: 4
  static const leftMini = EdgeInsets.only(left: 4);

  ///left: 8
  static const leftXs = EdgeInsets.only(left: 8);

  ///left: 16
  static const leftSm = EdgeInsets.only(left: 16);

  ///left: 24
  static const leftMd = EdgeInsets.only(left: 24);

  ///left: 32
  static const leftLg = EdgeInsets.only(left: 32);

  ///left: 40
  static const leftXl = EdgeInsets.only(left: 40);

  ///right: 4
  static const rightMini = EdgeInsets.only(right: 4);

  ///right: 8
  static const rightXs = EdgeInsets.only(right: 8);

  ///right: 16
  static const rightSm = EdgeInsets.only(right: 16);

  ///right: 24
  static const rightMd = EdgeInsets.only(right: 24);

  ///right: 32
  static const rightLg = EdgeInsets.only(right: 32);

  ///right: 40
  static const rightXl = EdgeInsets.only(right: 40);

  ///top: 4
  static const topMini = EdgeInsets.only(top: 4);

  ///top: 8
  static const topXs = EdgeInsets.only(top: 8);

  ///top: 16
  static const topSm = EdgeInsets.only(top: 16);

  ///top: 24
  static const topMd = EdgeInsets.only(top: 24);

  ///top: 32
  static const topLg = EdgeInsets.only(top: 32);

  ///top: 40
  static const topXl = EdgeInsets.only(top: 40);

  ///bottom: 4
  static const bottomMini = EdgeInsets.only(bottom: 4);

  ///bottom: 8
  static const bottomXs = EdgeInsets.only(bottom: 8);

  ///bottom: 16
  static const bottomSm = EdgeInsets.only(bottom: 16);

  ///bottom: 24
  static const bottomMd = EdgeInsets.only(bottom: 24);

  ///bottom: 32
  static const bottomLg = EdgeInsets.only(bottom: 32);

  ///bottom: 40
  static const bottomXl = EdgeInsets.only(bottom: 40);
}
