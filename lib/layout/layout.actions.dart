import 'package:flutter/material.dart';

class LayoutActions extends StatelessWidget {
  const LayoutActions({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
